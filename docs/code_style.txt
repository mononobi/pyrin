* maximum line length for every line is 100 characters.
  longer lines should be wrapped into multiple lines.

* application package and module names could be plural if needed.

* entity names and table names must be singular.

* in each class, every magic method must be implemented on top and after '__init__()'.