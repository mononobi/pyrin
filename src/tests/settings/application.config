[general]

# application title.
title: tests

# application base currency.
# example: USD, IRR, ...
base_currency: IRR

# application encoding.
encoding: utf-8

[logging]

# log level for flask default logger.
# this value will be bypassed after `pyrin.logging` package gets loaded.
flask_log_level: DEBUG
